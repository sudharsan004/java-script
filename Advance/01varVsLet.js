console.log(name);

var name = 'sudha';

console.log(fullName);

// let fullName = 'sudha';


// note we always use let in for loops- because the let will restrict the variable to the current scope only, outside of it the variable is not accessible.
for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
}
